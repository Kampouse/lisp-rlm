[package]
name = "json-lib"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["cdylib"]

[dependencies]
lite-json = "0.2"

[profile.release]
opt-level = "z"
lto = true
panic = "abort"
strip = true
